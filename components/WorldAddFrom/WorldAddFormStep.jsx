import React from "react";
import PropTypes from "prop-types";

function WorldAddFormStep({ children }) {
  return <>{children}</>;
}

export default WorldAddFormStep;

WorldAddFormStep.propTypes = {
  children: PropTypes.object,
};
